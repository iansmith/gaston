int main(void) {
    int *p;
    int i;
    p = malloc(40);
    i = 0;
    while (i < 5) {
        p[i] = (i + 1) * (i + 1);
        i = i + 1;
    }
    output(p[0]);
    output(p[2]);
    output(p[4]);
    free(p);
    return 0;
}
